<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      windowLayoutInDisplayCutoutMode only exists from API 27, so it lives in a
      v27 resource bucket rather than the base styles. Without it Android insets
      the whole window away from the camera cutout, which letterboxes the game
      and wastes a wide strip of a tall display. shortEdges lets the window
      extend into the cutout on the short edges — in landscape, the edge the
      camera sits on.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- The splash theme needs the same treatment, or the launch image is
         letterboxed and the game visibly jumps size when the WebView appears. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
